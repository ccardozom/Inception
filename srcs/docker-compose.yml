version: '3.5'

networks:
        inception:
                name: inception

services:
        nginx:
                build: requirements/nginx
                image: nginx:latest
                container_name: nginx
                restart: unless-stopped
                networks:
                        - inception
                ports:
                        # - 80:80
                        - 443:443
